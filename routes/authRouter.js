const Router = require('express');
const authController = require('../controllers/authControllers');
const router = Router();






// router.post("/",authController.delshop);
// router.post('/',authController.);
module.exports = router;